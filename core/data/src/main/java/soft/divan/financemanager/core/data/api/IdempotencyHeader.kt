package soft.divan.financemanager.core.data.api

/**
 * Заголовок, в котором мутирующие запросы несут ключ идемпотентности операции.
 *
 * Имя — де-факто отраслевой стандарт (Stripe и производные от него API). Значение берётся из
 * `OutboxEntryEntity.idempotencyKey`: оно генерируется один раз при постановке операции в очередь
 * и переживает все её повторы, поэтому сервер может отличить повторную доставку одной операции от
 * двух разных операций над одной сущностью.
 */
const val IDEMPOTENCY_KEY_HEADER = "Idempotency-Key"
