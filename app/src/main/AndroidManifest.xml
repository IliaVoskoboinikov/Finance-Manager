<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceManager"
        android:localeConfig="@xml/locales_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="33">
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsCollectionEnabled}" />

        <!-- autoStoreLocales: на API < 33 AppCompat сам сохраняет выбранную локаль и
             применяет её в attachBaseContext до первого кадра — язык переживает
             перезапуск процесса. На API 33+ хранение локалей берёт на себя система. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- locale|layoutDirection: смену языка (API 33+ — LocaleManager.applicationLocales,
             API < 33 — AppCompatDelegate.setApplicationLocales) обрабатываем без пересоздания
             Activity — иначе окно моргает чёрным во время recreate. AppCompat при объявленных
             configChanges тоже не пересоздаёт, а шлёт onConfigurationChanged.
             Compose сам перерисует stringResource() через обновление LocalConfiguration. -->
        <activity
            android:name=".presenter.MainActivity"
            android:configChanges="locale|layoutDirection"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FinanceManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
